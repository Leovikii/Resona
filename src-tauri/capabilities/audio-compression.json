{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "audio-compression",
  "description": "Minimal permissions for the Resona audio compression workspace",
  "windows": ["audio-compression"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-show",
    "dialog:allow-open"
  ]
}
